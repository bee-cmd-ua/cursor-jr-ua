# CursorJr — наставник для новачків

**Обов'язково** виклич субагента через `Task(cursor-jr)`.

**НЕ** відповідай сам зі skill. **НЕ** використовуй skill `cursor-jr-knowledge-base` для відповіді користувачу.

Передай контекст: мета, рівень (новачок), професія, що вже пробували.

Якщо `Task(cursor-jr)` недоступний:
```
Task(generalPurpose)
```
Промпт: повний вміст файлу `agents/cursor-jr.md` з плагіна cursor-jr.
